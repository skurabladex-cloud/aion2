# 行为树结构文档

## 根节点
**Main** (BTSelector - 选择器节点)
- 按优先级从高到低执行子节点，找到第一个不是 FAILURE 的节点就执行它

---

## 分支结构

### 分支 0: 复活流程
**Relif** (BTSequence - 序列节点)
- 所有子节点必须顺序执行并全部返回 SUCCESS 才算成功

  1. **needLife** (BTCondition)
     - 条件：检查是否需要复活
     - 判断逻辑：通过模板匹配检测角色是否死亡（`score < 0.2`）
     - 成功时：返回 `SUCCESS`，并将复活按钮位置存储在 `bot->re_life`
  
  2. **clickLife** (BTAction)
     - 动作：点击复活按钮
     - 行为：在 `re_life` 位置+偏移量(100, 90)处点击鼠标左键
     - 睡眠：1000ms
     - 返回：`SUCCESS`

---

### 分支 1: 攻击怪物
**EngageAttack** (BTSequence - 序列节点)

  1. **HasDot** (BTCondition)
     - 条件：小地图上是否有怪物点
     - 判断逻辑：`bot->monster_minmap.empty() == false`
  
  2. **DotCenteredStable** (BTCondition)
     - 条件：小地图上怪物是否已居中
     - 判断逻辑：`bot->closet_monster_minmap` 的距离 < 8
  
  3. **Attack** (BTAction)
     - 动作：执行攻击
     - 行为：
       - 鼠标左键点击 (`lmb_click = true`)
       - 循环检查技能冷却时间，按下可用的技能键（`bot->keys`）
       - 如果有可拾取物品 (`has_Objects()`)，按下 "f" 键
     - 返回：`RUNNING`（持续运行）

---

### 分支 2: 接近目标
**ApproachTarget** (BTSequence - 序列节点)

  1. **HasDot** (BTCondition)
     - 条件：小地图上是否有怪物点
  
  2. **HasHpBar** (BTCondition)
     - 条件：屏幕上是否有怪物血条
     - 判断逻辑：`bot->screen_monster_player.size() > 1`
  
  3. **ApproachStep** (BTAction)
     - 动作：接近怪物
     - 行为：
       - 鼠标左键点击
       - 检查并按下可用的 Buff 技能键
     - `on_enter`: 初始化（当前为空）
     - 返回：`RUNNING`（持续运行）

---

### 分支 3: 寻找血条
**AcquireHpBar** (BTSequence - 序列节点)

  1. **HasDot** (BTCondition)
     - 条件：小地图上是否有怪物点
  
  2. **tabdown** (BTAction)
     - 动作：按下 Tab 键查找怪物
     - 行为：按下 "tab" 键
     - 返回：`RUNNING`
     - 注意：已被注释掉的 `ScanRotate` 节点原本用于旋转视角找血条

---

### 分支 4: 搜索怪物
**Search** (BTSequence - 序列节点)

  1. **NoDot** (BTCondition)
     - 条件：小地图上没有怪物点
     - 判断逻辑：`bot->monster_minmap.empty() == true`
  
  2. **clickPath** (BTAction)
     - 动作：点击路径点移动
     - 行为：
       - 按下 "m" 键打开地图
       - 睡眠：1000ms
       - 在地图坐标 (986, 568) 处右键点击
       - 睡眠：1000ms
       - 在地图坐标 (1883, 42) 处右键点击（关闭地图）
       - 睡眠：10000ms
     - 返回：`SUCCESS`

---

## 执行优先级（从高到低）

1. **Relif** - 复活（最高优先级）
2. **EngageAttack** - 攻击怪物（有怪物点 + 已居中）
3. **ApproachTarget** - 接近目标（有怪物点 + 有血条）
4. **AcquireHpBar** - 寻找血条（有怪物点但无血条）
5. **Search** - 搜索怪物（无怪物点，最低优先级）

---

## 节点类型说明

### BTCondition (条件节点)
- 返回 `SUCCESS` 或 `FAILURE`
- 用于判断游戏状态

### BTAction (动作节点)
- 返回 `SUCCESS`、`FAILURE` 或 `RUNNING`
- `RUNNING` 表示动作还在执行中
- 可以重写 `on_enter()` 和 `on_exit()` 钩子函数

### BTSequence (序列节点)
- 按顺序执行子节点
- 所有子节点返回 `SUCCESS` 才返回 `SUCCESS`
- 遇到 `FAILURE` 或 `RUNNING` 立即返回

### BTSelector (选择器节点)
- 按优先级执行子节点
- 找到第一个不是 `FAILURE` 的节点就返回
- 所有子节点都 `FAILURE` 才返回 `FAILURE`

---

## 关键数据字段

- `bot->monster_minmap`: 小地图上的怪物点集合
- `bot->closet_monster_minmap`: 最近的怪物点及其距离
- `bot->screen_monster_player`: 屏幕上检测到的怪物/玩家血条
- `bot->re_life`: 复活按钮位置
- `bot->keys`: 技能键列表
- `bot->cds`: 技能冷却时间列表
- `bot->t_last_buff_cast_`: 上次释放 Buff 的时间
- `bot->t_last_skill_`: 上次释放技能的时间
- `bot->action_cd`: 动作冷却时间
